"use strict";
const books = ['books1', 'book2', 'book3'];
const runs = [10, 20, 30, 40, 50];
const shoppingList = ['iphone', 15000]; //tuple
const book = {
    name: 'chemistry',
    author: 'Nagg',
    price: 500,
    pages: 200,
    // isUsed:true
};
