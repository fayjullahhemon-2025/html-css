// 3. Given two strings (first name and last name), combine them into a full name using a template literal and print a greeting
// message.

const firstName = "Fayjullah Haque";
const lastName = "Emon";
const fullName = `${firstName} ${lastName}`;
console.log(fullName);