// 2. Given a string with leading and trailing spaces, print its length before trimming and after trimming to show the
// difference.

const str = "   Emon   ";
console.log("Before trimming ",str.length);
console.log("After trimming ",str.trim().length);