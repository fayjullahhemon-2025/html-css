// Task 5.1 — Build a Profile Object
// Create an object student with these properties: name, age, grade, and isEnrolled.
// Log the whole object.
// Log just the name property using dot notation.
console.log(" Task 5.1 — Build a Profile Object");

const student = {
    name: "Fayjullah Haque Emon",
    age : 26,
    grade: 3.62,
    isEnrolled: true
};
console.log(student);
console.log(student.name);

// Task 5.2 — Watch the Video Concept
// After watching the "What is an Object" video, write 2–3 sentences (as a comment in your code) explaining an object in your own words, using a real-life analogy (not a car or a person — get creative!).

console.log("Task 5.2 — Watch the Video Concept");
console.log("Object is a datatype that can store values as key and value pair.");
console.log("Suppose Mobile as an object.");
console.log("Mobile have display, buttons, their workings like display shows UI and different buttons have different functionality which are called method. ");